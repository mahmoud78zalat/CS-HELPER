[phases.build]
dependsOn = ["install"]
cmds = ["npm run build"]

[phases.install]
dependsOn = ["setup"]
cmds = ["npm ci --include=dev"]

[phases.setup]
nixpkgs = ["nodejs-20_x", "npm-9_x"]

[start]
cmd = "npm run start"

[variables]
NODE_ENV = "production"
PORT = "8080"