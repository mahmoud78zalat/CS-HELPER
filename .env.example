# Customer Service Platform - Environment Variables

# === REQUIRED FOR RENDER.COM DEPLOYMENT ===

# Node.js Environment
NODE_ENV=production
PORT=5000

# Session Configuration (Generate a secure random string)
SESSION_SECRET=your-secure-session-secret-here

# === DATABASE OPTIONS ===

# Option 1: Use Render PostgreSQL (automatic)
# DATABASE_URL will be automatically set by Render when using their PostgreSQL service

# Option 2: Use Supabase (recommended for advanced features)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# === OPTIONAL CONFIGURATIONS ===

# Replit Integration (only needed for Replit deployment)
REPL_ID=your-repl-id
ISSUER_URL=https://identity.replit.com

# === ENVIRONMENT VARIABLE NOTES ===

# For Render.com deployment:
# 1. Copy this file to .env in production
# 2. Set all required variables in Render dashboard
# 3. SESSION_SECRET should be a random 32+ character string
# 4. Choose either Render PostgreSQL OR Supabase for database
# 5. If using Supabase, get credentials from your Supabase project settings

# Security Notes:
# - Never commit actual values to version control
# - Use Render's environment variable dashboard for production
# - Rotate keys regularly for production applications
# - Test all variables after deployment